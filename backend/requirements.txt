# Core Django
Django==5.0.6
djangorestframework==3.15.2
django-cors-headers==4.4.0
django-filter==24.2

# Database
psycopg2-binary==2.9.9

# Task Queue
celery==5.4.0
redis==5.0.8
django-celery-beat==2.7.0
django-celery-results==2.5.1

# PDF Generation
# WeasyPrint 62.3 broken: pdf/stream.py calls Stream.transform() removed in pydyf 0.10+.
# WeasyPrint 57.2 broken with pydyf 0.11+: PDF.__init__ API changed (no positional args).
# Fix: pin both to compatible versions — weasyprint==57.2 + pydyf==0.8.0.
# pydyf 0.8.0: PDF(version, identifier) positional args ✓, Stream.transform() ✓
weasyprint==57.2
pydyf==0.8.0

# Authentication & Security
djangorestframework-simplejwt==5.3.1

# Mobile push notifications (Firebase Cloud Messaging)
firebase-admin==6.5.0

# Error monitoring
sentry-sdk[django,celery]==2.14.0

# Storage
django-storages==1.14.4
boto3==1.35.0

# Config Management
python-decouple==3.8

# API Documentation
drf-spectacular==0.27.2

# Image Processing
Pillow==10.4.0

# Utilities
django-extensions==3.2.3
python-dateutil==2.9.0
babel==2.15.0

# Admin
django-admin-interface==0.28.6
django-colorfield==0.11.0
dj-database-url==2.2.0
