[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — all paths serve index.html, React Router handles the rest
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
