services:
  - type: web
    name: shodz-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile.render
    dockerContext: ./backend
    plan: free
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.render
      - key: DEPLOYMENT_MODE
        value: cloud
      - key: STORAGE_PROVIDER
        value: local
      - key: SECRET_KEY
        generateValue: true
      # Superuser created automatically on first deploy
      - key: DJANGO_SUPERUSER_EMAIL
        sync: false
      - key: DJANGO_SUPERUSER_PASSWORD
        sync: false
      # External services — set from Neon and Upstash dashboards
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: ALLOWED_HOSTS
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        sync: false
